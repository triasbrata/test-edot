import { SentryGlobalFilter } from './sentry-global.filter';

describe('SentryGlobalFilterFilter', () => {
  it('should be defined', () => {
    expect(new SentryGlobalFilter()).toBeDefined();
  });
});

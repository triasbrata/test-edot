export enum EnvRedis {
  REDIS_PORT = 'REDIS_PORT',
  REDIS_IP = 'REDIS_IP',
}

export enum InjectName {
  REDIS_PORT = 'REDIS_PORT',
  REDIS_IP = 'REDIS_IP',
}

export enum RedisKeys {
  WarehouseReserve = 'warehouse_stock:reserve:%s',
}
